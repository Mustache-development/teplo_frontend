[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "build"

[[headers]]
  for = "/*" # ви можете вказати конкретні шляхи тут
  [headers.values]
    access-control-allow-origin = "*"